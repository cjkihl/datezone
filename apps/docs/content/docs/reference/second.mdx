---
title: Second
---

## `startOfSecond`

Returns the timestamp at the start of the second (milliseconds set to 0).

```ts
import { startOfSecond } from "@datezone/second";
```

<CodeExample file="reference/second/startOfSecond.ts" title="startOfSecond" />

## `endOfSecond`

Returns the timestamp at the end of the second (999 milliseconds).

```ts
import { endOfSecond } from "@datezone/second";
```

<CodeExample file="reference/second/endOfSecond.ts" title="endOfSecond" />

## `addSeconds`

Adds a specified number of seconds to a timestamp.

```ts
import { addSeconds } from "@datezone/second";
```

<CodeExample file="reference/second/addSeconds.ts" title="addSeconds" />

## `subSeconds`

Subtracts a specified number of seconds from a timestamp.

```ts
import { subSeconds } from "@datezone/second";
```

<CodeExample file="reference/second/subSeconds.ts" title="subSeconds" />

## `addMilliseconds`

Adds a specified number of milliseconds to a timestamp.
This is the most precise time addition operation available.

```ts
import { addMilliseconds } from "@datezone/second";
```

<CodeExample file="reference/second/addMilliseconds.ts" title="addMilliseconds" />

## `subMilliseconds`

Subtracts a specified number of milliseconds from a timestamp.
This is the most precise time subtraction operation available.

```ts
import { subMilliseconds } from "@datezone/second";
```

<CodeExample file="reference/second/subMilliseconds.ts" title="subMilliseconds" />
