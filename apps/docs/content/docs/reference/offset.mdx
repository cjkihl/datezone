---
title: Offset
---

## `getUTCtoTimezoneOffsetMinutes`

Returns the offset in minutes between UTC and the given time zone at the given date.
Positive if the zone is ahead of UTC, negative if behind.
Uses a per-hour cache for performance.
If no timeZone is specified, defaults to local system timeZone.

```ts
import { getUTCtoTimezoneOffsetMinutes } from "@datezone/offset";
```

<CodeExample file="reference/offset/getUTCtoTimezoneOffsetMinutes.ts" title="getUTCtoTimezoneOffsetMinutes" />

## `getLocalTimezoneOffsetMinutes`

Fast path for local timeZone offset calculation.
Uses native Date.getTimezoneOffset() for better performance when working with the system timeZone.

```ts
import { getLocalTimezoneOffsetMinutes } from "@datezone/offset";
```

<CodeExample file="reference/offset/getLocalTimezoneOffsetMinutes.ts" title="getLocalTimezoneOffsetMinutes" />

## `getTimezoneOffsetMinutes`

Returns the offset in minutes from fromZone to toZone at the given date.
Fast path for UTC zones to avoid extra formatToParts calls.
If timeZone parameters are not specified, defaults to local system timeZone.

```ts
import { getTimezoneOffsetMinutes } from "@datezone/offset";
```

<CodeExample file="reference/offset/getTimezoneOffsetMinutes.ts" title="getTimezoneOffsetMinutes" />

## `getFixedOffsetCacheInfo`

Returns information about the fixed offset cache for debugging purposes.

```ts
import { getFixedOffsetCacheInfo } from "@datezone/offset";
```

<CodeExample file="reference/offset/getFixedOffsetCacheInfo.ts" title="getFixedOffsetCacheInfo" />

## `clearFixedOffsetCache`

Clears the fixed offset cache. Useful for testing or memory management.

```ts
import { clearFixedOffsetCache } from "@datezone/offset";
```

<CodeExample file="reference/offset/clearFixedOffsetCache.ts" title="clearFixedOffsetCache" />
