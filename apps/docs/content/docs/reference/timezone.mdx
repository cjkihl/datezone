---
title: Timezone
---

## `UTCTimeZone`

Union type listing all IANA zone names that have a fixed **UTC±00:00** offset (no DST).

<CodeExample file="reference/timezone/UTCTimeZone.ts" title="UTCTimeZone" />

## `DSTTimeZone`

Union type listing IANA zones that observe **Daylight Saving Time** and therefore change their offset during the year.

<CodeExample file="reference/timezone/DSTTimeZone.ts" title="DSTTimeZone" />

## `OtherTimeZone`

Union type for every IANA zone that is neither purely UTC nor uses DST (fixed non-zero offset).

<CodeExample file="reference/timezone/OtherTimeZone.ts" title="OtherTimeZone" />

## `TimeZone`

Combined union of `UTCTimeZone | DSTTimeZone | OtherTimeZone`; represents **any** valid time-zone string accepted by Datezone.

<CodeExample file="reference/timezone/TimeZone.ts" title="TimeZone" />

## `isUTC`

Checks if a timeZone is UTC.

<CodeExample file="reference/timezone/isUTC.ts" title="isUTC" />

## `isDST`

Checks if a timeZone is a DST timeZone. (DST = Daylight Saving Time)
If the timeZone do not have DST we can do simple arithmetic to get the correct time.

<CodeExample file="reference/timezone/isDST.ts" title="isDST" />
