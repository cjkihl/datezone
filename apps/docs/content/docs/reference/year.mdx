---
title: Year
---

## `year`

Extracts the year from a timestamp.

```ts
import { year } from "@datezone/year";
```

<CodeExample file="reference/year/year.ts" title="year" />

## `isLeapYear`

Checks if the year containing the given timestamp is a leap year.

```ts
import { isLeapYear } from "@datezone/year";
```

<CodeExample file="reference/year/isLeapYear.ts" title="isLeapYear" />

## `isLeapYearBase`

Checks if a given year is a leap year.

```ts
import { isLeapYearBase } from "@datezone/year";
```

<CodeExample file="reference/year/isLeapYearBase.ts" title="isLeapYearBase" />

## `startOfYear`

Gets the timestamp for the start of the year containing the given timestamp.

```ts
import { startOfYear } from "@datezone/year";
```

<CodeExample file="reference/year/startOfYear.ts" title="startOfYear" />

## `endOfYear`

Gets the timestamp for the end of the year containing the given timestamp.

```ts
import { endOfYear } from "@datezone/year";
```

<CodeExample file="reference/year/endOfYear.ts" title="endOfYear" />

## `addYears`

Adds the specified number of years to a timestamp.
Handles leap year dates appropriately (e.g., Feb 29 -> Feb 28 in non-leap years).

```ts
import { addYears } from "@datezone/year";
```

<CodeExample file="reference/year/addYears.ts" title="addYears" />

## `subYears`

Subtracts the specified number of years from a timestamp.

```ts
import { subYears } from "@datezone/year";
```

<CodeExample file="reference/year/subYears.ts" title="subYears" />

## `daysInYear`

Gets the number of days in the year containing the given timestamp.

```ts
import { daysInYear } from "@datezone/year";
```

<CodeExample file="reference/year/daysInYear.ts" title="daysInYear" />

## `daysInYearBase`

Gets the number of days in the given year.

```ts
import { daysInYearBase } from "@datezone/year";
```

<CodeExample file="reference/year/daysInYearBase.ts" title="daysInYearBase" />

## `quarter`

Returns the quarter of the year (1-4) for the given month.

```ts
import { quarter } from "@datezone/year";
```

<CodeExample file="reference/year/quarter.ts" title="quarter" />
