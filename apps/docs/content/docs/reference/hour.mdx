---
title: Hour Utilities
description: Functions for working with hours, minutes, seconds, and milliseconds
---

import { CodeExample } from '@/components/code-example';

# Hour Utilities

Hour utilities provide functions for working with hours, minutes, seconds, and milliseconds in dates and timestamps, including time arithmetic and time boundaries.

## get12Hour

Get the hour in 12-hour format (1-12).

```typescript
function get12Hour(ts: OptionsOrTimestamp, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/get-12-hour-local.ts" },
  { name: "With timezone", file: "reference/hour/get-12-hour-timezone.ts" }
]} />

## get24Hour

Get the hour in 24-hour format (0-23).

```typescript
function get24Hour(ts: OptionsOrTimestamp, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/get-24-hour-local.ts" },
  { name: "With timezone", file: "reference/hour/get-24-hour-timezone.ts" }
]} />

## getHour

Get the hour (same as get24Hour).

```typescript
function getHour(ts: OptionsOrTimestamp, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/hour-local.ts" },
  { name: "With timezone", file: "reference/hour/hour-timezone.ts" }
]} />

## addHours

Add hours to a timestamp.

```typescript
function addHours(ts: OptionsOrTimestamp, hours: number, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/add-hours-local.ts" },
  { name: "With timezone", file: "reference/hour/add-hours-timezone.ts" }
]} />

## subHours

Subtract hours from a timestamp.

```typescript
function subHours(ts: OptionsOrTimestamp, hours: number, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/sub-hours-local.ts" },
  { name: "With timezone", file: "reference/hour/sub-hours-timezone.ts" }
]} />

## startOfHour

Get the timestamp for the start of the hour.

```typescript
function startOfHour(ts: OptionsOrTimestamp, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/start-of-hour-local.ts" },
  { name: "With timezone", file: "reference/hour/start-of-hour-timezone.ts" }
]} />

## endOfHour

Get the timestamp for the end of the hour.

```typescript
function endOfHour(ts: OptionsOrTimestamp, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/end-of-hour-local.ts" },
  { name: "With timezone", file: "reference/hour/end-of-hour-timezone.ts" }
]} />

## startOfMinute

Get the timestamp for the start of the minute.

```typescript
function startOfMinute(ts: OptionsOrTimestamp, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/start-of-minute-local.ts" },
  { name: "With timezone", file: "reference/hour/start-of-minute-timezone.ts" }
]} />

## endOfMinute

Get the timestamp for the end of the minute.

```typescript
function endOfMinute(ts: OptionsOrTimestamp, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/end-of-minute-local.ts" },
  { name: "With timezone", file: "reference/hour/end-of-minute-timezone.ts" }
]} />

## startOfSecond

Get the timestamp for the start of the second.

```typescript
function startOfSecond(ts: OptionsOrTimestamp, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/start-of-second-local.ts" },
  { name: "With timezone", file: "reference/hour/start-of-second-timezone.ts" }
]} />

## endOfSecond

Get the timestamp for the end of the second.

```typescript
function endOfSecond(ts: OptionsOrTimestamp, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/end-of-second-local.ts" },
  { name: "With timezone", file: "reference/hour/end-of-second-timezone.ts" }
]} />

## addMinutes

Add minutes to a timestamp.

```typescript
function addMinutes(ts: OptionsOrTimestamp, minutes: number, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/add-minutes-local.ts" },
  { name: "With timezone", file: "reference/hour/add-minutes-timezone.ts" }
]} />

## subMinutes

Subtract minutes from a timestamp.

```typescript
function subMinutes(ts: OptionsOrTimestamp, minutes: number, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/sub-minutes-local.ts" },
  { name: "With timezone", file: "reference/hour/sub-minutes-timezone.ts" }
]} />

## addSeconds

Add seconds to a timestamp.

```typescript
function addSeconds(ts: OptionsOrTimestamp, seconds: number, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/add-seconds-local.ts" },
  { name: "With timezone", file: "reference/hour/add-seconds-timezone.ts" }
]} />

## subSeconds

Subtract seconds from a timestamp.

```typescript
function subSeconds(ts: OptionsOrTimestamp, seconds: number, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/sub-seconds-local.ts" },
  { name: "With timezone", file: "reference/hour/sub-seconds-timezone.ts" }
]} />

## addMilliseconds

Add milliseconds to a timestamp.

```typescript
function addMilliseconds(ts: OptionsOrTimestamp, ms: number, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/add-milliseconds-local.ts" },
  { name: "With timezone", file: "reference/hour/add-milliseconds-timezone.ts" }
]} />

## subMilliseconds

Subtract milliseconds from a timestamp.

```typescript
function subMilliseconds(ts: OptionsOrTimestamp, ms: number, timeZone?: TimeZone): number
```

<CodeExample tabs={[
  { name: "Local time", file: "reference/hour/sub-milliseconds-local.ts" },
  { name: "With timezone", file: "reference/hour/sub-milliseconds-timezone.ts" }
]} /> 